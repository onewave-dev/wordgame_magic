# Telegram Bot «Составь слово!»

Сделать Telegram-бота с вебхуком (**FastAPI**), реализующего игру по описанной ниже логике.

ПРАВИЛА ИГРЫ «Составь слово!»: 

Придумывается инициирующим игроком или выбирается случайным образом автоматически и отображается длинное слово (не менее 8 букв). 
Игрокам сообщается исходное слово, которое в игре отображается всегда перед глазами (нижняя кнопка со словом в uppercase, приходящая как постоянное наиболее последнее сообщение в чате игры). 
Игрокам дается заданное время на то, чтобы составить из букв этого слова как можно больше других слов, состоящих минимум из трех букв. Например, из букв слова вертолёт можно составить слова вор, лев, рёв, рот, лето, торт, ответ, тело, отлёт, ров. 
Побеждает игрок, который напишет наибольшее количество слов существительных (имена собственные и существительные во множественном числе -  кроме тех существительных множественного числа, у которых не существует формы единственного числа -  писать не разрешается). Слово из шести и более букв засчитывается за два слова. В конце игры объявляется победитель.

 🎯 Цель

Сделать Telegram-бота с вебхуком (**FastAPI**), который:

- работает для количества участников от 2 до 5 в одном групповом чате;
- даёт выбор длительности игры: **3 минуты** или **5 минут**;
- автоматически шлёт предупреждение **за 1 минуту** до конца;
- в начале даёт инициатору кнопки приглашения участников: выбор из контактов (используя стандартный функционал телеграм) или создание ссылки для приглашения;
- до вывода меню с дальнейшими действиями, запрашивает имя у каждого игрока при входе в игру и использует его дальше везде в игре;
- реализует правила игры **«Составь слово!»** (описаны выше).


Игроки присоединяются в тот же групповой чат, где идёт игра.

---

## 🏗 Архитектура

- **FastAPI**: обрабатывает  
  - `/webhook` (POST)  
  - служебные маршруты `/healthz` и `/set_webhook`
- **python-telegram-bot v20**:  
  - создаём `Application` в lifecycle FastAPI  
  - запускаем **job queue** (таймеры)  
  - принимаем апдейты через `application.process_update(update)`
- Состояние игры хранится в памяти (`ACTIVE_GAMES` по `chat_id`)  
  - Для Render Free этого достаточно (пока процесс жив).  
  - Для персистентности — можно позже подключить БД.
- Дополнительные требования архитектуры:
  - добавить необходимый endpoint для /healthz
  - убедиться, что не удаляется вебхук при обычном выключении сервиса. Пусть запись о вебхуке остаётся в Telegram — тогда любое сообщение пользователя вызовет попытку HTTP‑доставки, Render поднимет контейнер
  - build in auto registrring webhook at start-up (conditionally, of re-registration is necessary)
  - schedule periodic webhook health checks inside the program

---

## 📦 Основные сущности  (!добавить дополнительные по необходимости!)

### GameState (на чат)
- `host_id`   
- `time_limit` (3 | 5)  
- `base_word`  
- `letters` (`Counter`)  
- `players` (`user_id → Player`)  
- `used_words`  
- `status` (config | waiting | running | finished)  
- `jobs` (warn / end)

### Player
- `user_id`  
- `name`  
- `words`  
- `points`

### Прочее
- `JOIN_CODES` — карта для deep-link приглашений.

---

## ▶️ Валидация слов
Слова для выбора базового слова, а также слова, которые зачитываются в качестве корректных от участников валидируются по словарю nouns_ru_pymorphy2_yaspeller.jsonl, сохраненному в репозитории.

е и ё в игре считаются эквивалентными буквами. Регист букв в игре игнорируется

## ▶️ Поток игры

1. Инициатор → команда `/newgame` (или меню «Новая игра»).
1.1. Ввод имени игрока (до появления дальнейших меню)
2. Выбор:  
   - **3 или 5 минут**  
3. Бот публикует:  
   - Кнопки «Пригласить из контактов", "Создать ссылку" (inline)и  «Присоединиться» для участников  
   - deep-link для пересылки друзьям
4. Игроки жмут «Присоединиться».  
   Если имени нет → бот запрашивает (через ForceReply). Ввод имени всех игроков обязателен для появления дальнейших меню.
5. Когда игроков в игре не менее 2х →  у иницииатора появляются кнопки опций выбора базового слова: вручную (≥8 букв) или случайное.
5.1.  При выборе "Случайное":
 -- дополнительный критерий выбора: для игры 5 минут или дольше слово должно состоять из не менее 10  букв
 -- дополнительный критерий выбора: если в игру подключилось 3 или более участников, слово должно состоять из не менее 9  букв
 -- телеграм-бот выбирает 3 валидных слова и выводит в виде кнопок для выбора участникам. У участников есть 5 секунд на выбор. Нижним сообщением отсчитывается таймер от 5 до 1.
 -- слово нажатое первым участником считается выбранным для игры
 -- если никакой участник не нажал, игра выбирает случайное из этих трех слов
 -- отображается всем игрокам сообщение что {имя игрока} выбрал слово {базовое слово}
6. инициатор жмёт «Старт».

6.1. Бот объявляет исходное слово и запускает внутренний таймер.  
7. Игроки присылают слова свободно (не по ходам). Можно одно слово или несколько слов пачкой через пробел.   
7.1. Проверка слова:  
   - только кириллица  
   - ≥3 буквы  
   - из букв исходного слова (по частотам)  
   - не повтор  
   - слово присутствует в валидационном словаре (см. выше в "Валидация слов")
   - длина ≥6 букв = **2 очка**, иначе 1
8. За 1 минуту — предупреждение.
9. По окончании игры — финальный подсчёт, объявление победителя.
10. Затем - вывод в чат опций: 1) "Новая игра с теми же участниками" (да/нет).  --> использует уже приглашенных участников и переводит в меню выбора параметров игры. 2) Сообщение: Для новой игры с новыми участниками нажмите /start. --> создается  новая сессия игры

---
## 📦 Тестовая версия игры

ДОБАВИТЬ ТЕСТОВОУЮ АДМИН ВЕРСИЮ ИГРЫ  ПО КНОПКЕ,  ДОСТУПНОЙ ТОЛЬКО АДМИНУ

Инструкции по тестовой админ версии игры:
 -- ADMIN_ID задает телеграм ID администратора
 -- админ кнопка с предваряющей надписью "[адм.]" в начале названия, появляется в первом же меню выбора параметров игры. Кнопка видна только администратору
 -- тестовая игра имитирует основную игру для живых участников, она  идентична основной игре для живых участников, со следующими различиями:
	- за одного участника играет  бот, который подключен к игре с самого начала автоматически;
	- бот имитирует игру живого участника, отправляя в игру валидное слово один раз в 30 секунд.


---

## 📂 Структура проекта

```
.
├── app.py            # FastAPI + PTB v20, логика игры
├── requirements.txt  # зависимости
├── AGENTS.md         # краткая инструкция по деплою
├── nouns_ru_pymorphy2_yaspeller.jsonl  # встроенный словарь для валидации слов


```

---

## 📜 requirements.txt

```
python-telegram-bot>=20.6
fastapi>=0.110
uvicorn[standard]>=0.30
```

---

## ⚡ Быстрый старт локально

1. Создайте `.env` или экспортируйте переменные окружения.
2. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```
3. Запустите сервер:
   ```
   uvicorn app:app --host 0.0.0.0 --port 8000
   ```
4. Для вебхука нужен туннель (ngrok / cloudflared).  
   Установите `PUBLIC_URL` и вызовите:
   ```
   GET /set_webhook
   ```

---

## 🚀 Деплой на Render (Free)  -  !!!уже осуществлен хозяином репозитория!!! 

1. Создайте **Web Service** → подключите GitHub-репозиторий.
2. Настройки:
   - Build Command:
     ```
     pip install -r requirements.txt
     ```
   - Start Command:
     ```
     uvicorn app:app --host 0.0.0.0 --port $PORT
     ```
   - Добавьте переменные окружения (см. ниже).
3. После деплоя откройте:
   ```
   https://<service>.onrender.com/set_webhook
   ```
   (или используйте авто-установку в lifecycle).
4. Добавьте бота в группу → `/newgame`.



## ⚙️ Переменные окружения (!уже заданы на render владельцем игры!)

### Обязательные
- `ADMIN_ID` - телегам-ID администратора
- `TELEGRAM_BOT_TOKEN` — токен от @BotFather  
- `PUBLIC_URL` — публичный HTTPS-URL (например, `https://<имя>.onrender.com`)  
- `WEBHOOK_SECRET` — длинная случайная строка 
- `WEBHOOK_PATH` — (опц.) путь вебхука, по умолчанию `/webhook`

